{
  "scripts": {
    "site:dev": "wrangler pages dev site --compatibility-date=2026-04-29",
    "site:deploy": "wrangler pages deploy site --project-name zxcaml --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^4.76.0"
  }
}
