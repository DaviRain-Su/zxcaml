== Compute units (static estimate) ==
estimated cu budget: 1
breakdown:
  - syscalls:
      (none)
  - arithmetic ops: 1  (≈ 1 cu, free in BPF but counted for completeness)
  - control flow: 0
  - function calls: 0 non-syscall
risks:
  - has unknown dynamic loop: __zxc_loop_0 (while) in entrypoint
notes:
  - This is a STATIC estimate based on the post-optimization Core IR. Real BPF compute units are determined at runtime by the SVM and may differ.
  - WARN: at least one recursive call is dynamic and not statically bounded.
  - See https://docs.solana.com/developing/programming-model/runtime for the canonical cost table.

== Max function stack depth (static estimate) ==
deepest function: entrypoint ≈ 0 bytes
all functions:
  entrypoint: 0 bytes
notes:
  - BPF stack frame limit is 4096 bytes per call (SBPFv2 spec, subject to validator).
  - This counts named locals + params at their Lowered IR layout; closure captures stored in arena are NOT counted here.
  - Closures captured into the heap are not stack-relevant; this estimate covers true stack frames only.
